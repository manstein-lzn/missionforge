# Module: Verification and Repair

## Goal

Verify mission evidence and convert failures into structured repair contracts.

## Scope

- validator result schema
- failed constraint record
- missing evidence record
- validator mode and severity semantics
- verification profile language validation
- repair contract
- redesign contract
- review and authority gates
- reviewer decision validation
- failed proposal and observation-signal handling as non-proof evidence

## Non-Goals

- no task-name verifier branches
- no log-string repair routing
- no LLM interpretation as hard proof
- no reviewer approval replacing failed executable validators

## Current Status

Phase 1 verification contract primitives exist. Phase 3 added the first
deterministic verifier kernel, local validator dispatch, and reviewer decision
validation. Repair planner and redesign protocol remain design-only.

Implemented in Phase 1:

- `ValidatorSpec`
- `VerificationSpec`
- `ValidatorResult`
- `VerificationResult`
- JSON-compatible round-trip helpers for implemented contracts

Implemented in Phase 3:

- `FailedConstraint`
- `MissingEvidence`
- `ReviewerDecision`
- `Verifier`
- `verify_spec()`
- deterministic validators: `file_exists`, `file_contains`, `forbidden_path`,
  `json_field_exists`, `artifact_hash`, and `command`
- routing for `failed`, `review_required`, `human_acceptance_required`,
  `unsupported_verification_spec`, and `completed_verified`

## Public Contracts

- `ValidatorResult`
- `VerificationResult`
- `FailedConstraint`
- `MissingEvidence`
- `ReviewerDecision`

To be designed:

- `RepairContract`
- `RedesignRequest`
- `ReviewGate`
- `VerificationProfile`
- `ValidatorLanguageSpec`

## Invariants

- Every failure should cite a Mission IR constraint ID when possible.
- Repair contracts are generated from structured failures.
- Manual authority gates are explicit and cannot be auto-approved.
- Stale reviewer decisions and worker-authored reviewer approvals are rejected.
- Worker self-report is never acceptance.
- LLM self-report and proposal confidence are never acceptance.
- Blocking unsupported verification requires redesign or explicit authority
  escalation.
- Advisory failures are surfaced as warnings, not hard proof.
- Delegatable manual gates route to `review_required` and can resume only with
  an independent, current reviewer decision.
- Human-only gates route to `human_acceptance_required` only when explicitly
  reserved by the frozen contract.

## Dependencies

- Mission IR
- context/evidence
- harness reports
- controlled steering records

## Verification Strategy

- failing constraint creates failed constraint record
- failed constraint creates repair contract
- repair loop does not inspect free-form failure text
- executable/manual/unsupported validator modes route to distinct statuses
- blocking/advisory severities affect completion status correctly
- stale reviewer decision is rejected
- worker-authored reviewer decision is rejected
- LLM observation signal cannot override verifier failure
- unsupported blocking validators route redesign or authority escalation

## Verification Evidence

Phase 1:

```bash
PYTHONPATH=src python3 -m unittest tests/test_verification_contracts.py
# Ran 4 tests: OK
```

Phase 3:

```bash
PYTHONPATH=src python3 -m unittest tests/test_validators.py tests/test_verifier.py tests/test_reviewer_decision.py
# Ran 17 tests: OK
```

## Open Questions

- How are generated tests represented as validators?
- How are unverifiable requirements escalated?
- Which generic validators ship in the first verification profile registry?
- Should review packets be generated by verifier, runtime, or controlled
  steering?
