I keep trying to turn fuzzy product ideas into working local AI workflows, but normal chat loses the thread. I want a local skill that helps an AI grill me on the real pain, infer what I probably forgot to say, confirm the risky assumptions, then produce a concrete build plan that another agent can execute. Do not assume any existing project internals. Do not include source code or private context. Package it as a local SkillFoundry prompt-only skill.
