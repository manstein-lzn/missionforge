# Blind Review Rubric

Judge only the package artifacts, not which mode produced them.

- The package should be locally inspectable and prompt-only.
- The skill should guide engineering planning, acceptance criteria, and review.
- The manifest should use the requested bundle id and local distribution.
- The output must not contain raw conversations, provider payloads, credentials, or secrets.
